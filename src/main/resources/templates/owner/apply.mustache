<!doctype html>
<html lang="ko"><head>
    <meta charset="utf-8"><title>점주 신청</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head><body class="bg-body-tertiary">
{{> layout/header}}

<main class="container py-5" style="max-width:720px;">
    <div class="card shadow-sm">
        <div class="card-body p-4">
            <h4 class="mb-4">점주 신청</h4>
            {{#errorMessage}}<div class="alert alert-danger">{{.}}</div>{{/errorMessage}}

            <form method="post" action="/owner/apply">
                {{#_csrf}}<input type="hidden" name="{{parameterName}}" value="{{token}}">{{/_csrf}}

                <div class="mb-3">
                    <label class="form-label">가게 유형</label>
                    <select class="form-select" name="storeType" required>
                        <option value="">선택하세요</option>
                        <option value="CAFE">카페</option>
                        <option value="RESTAURANT">맛집</option>
                    </select>
                </div>

                <div class="mb-3">
                    <label class="form-label">가게 이름</label>
                    <input type="text" name="storeName" class="form-control" required>
                </div>

                <div class="mb-3">
                    <label class="form-label">사업자 등록번호</label>
                    <input type="text" name="businessNumber" class="form-control" placeholder="예) 123-45-67890" required>
                </div>

                <div class="mb-3">
                    <label class="form-label">대표자 본명</label>
                    <input type="text" name="ownerRealName" class="form-control" required>
                </div>

                <div class="mb-3">
                    <label class="form-label">연락처</label>
                    <input type="text" name="contactPhone" class="form-control" placeholder="예) 010-1234-5678" required>
                </div>

                <div class="mb-3">
                    <label class="form-label">가게 주소</label>
                    <input type="text" name="storeAddress" class="form-control" placeholder="도로명 주소" required>
                </div>

                <div class="d-flex gap-2">
                    <a class="btn btn-outline-secondary" href="/mypage">돌아가기</a>
                    <button class="btn btn-primary ms-auto" type="submit">신청하기</button>
                </div>
            </form>
        </div>
    </div>
</main>

{{> layout/footer}}
</body></html>
