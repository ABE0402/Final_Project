<!doctype html>
<html lang="ko">
<head>
    <meta charset="utf-8"><title>마이페이지 - 예약내역</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/app.css" rel="stylesheet">
</head>
<body class="bg-body-tertiary">
{{> layout/header}}

<main class="container py-5">
    <h3 class="fw-bold mb-4">예약 내역</h3>
    <div class="row g-4">
        <aside class="col-12 col-md-3">{{> mypage/sidebar}}</aside>

        <section class="col-12 col-md-9">
            <div class="table-responsive">
                <table class="table bg-white align-middle">
                    <thead>
                    <tr><th>가게</th><th>예약일시</th><th>인원</th><th>상태</th><th></th></tr>
                    </thead>
                    <tbody>
                    {{#reservations}}
                        <tr>
                            <td>{{placeName}}</td>
                            <td>{{reservedAt}}</td>
                            <td>{{partySize}}</td>
                            <td>{{status}}</td>
                            <td><button class="btn btn-outline-danger btn-sm disabled" disabled>취소</button></td>
                        </tr>
                    {{/reservations}}
                    {{^reservations}}
                        <tr><td colspan="5" class="text-center text-muted">예약 내역이 없습니다.</td></tr>
                    {{/reservations}}
                    </tbody>
                </table>
            </div>
        </section>
    </div>
</main>

{{> layout/footer}}
</body>
</html>
