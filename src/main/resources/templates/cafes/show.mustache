<!-- templates/cafes/show.mustache -->
<!doctype html>
<html lang="ko">
<head>
    <meta charset="utf-8">
    <title>{{#cafe}}{{name}}{{/cafe}} - Hong Around</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/app.css" rel="stylesheet">
    <style>
        .thumb-rail img{object-fit:cover;width:100%;height:140px;border-radius:.5rem}
        .map-box{height:280px;background:#eef1e6;border:1px solid #dbe3c0;border-radius:.5rem}
        .badge-olive{background:#5b7c38}
    </style>
</head>
<body class="bg-body-tertiary">
{{> layout/header}}

<main class="container py-4">

    <!-- 상단 헤더 -->
    <div class="row g-3 mb-3">
        <div class="col-12 col-lg-8">
            <div class="card shadow-sm">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-start">
                        <div>
                            <h4 class="mb-1">{{#cafe}}{{name}}{{/cafe}}</h4>
                            <div class="small text-muted">
                                ⭐ {{avg}} · 리뷰 {{#cafe}}{{reviewCount}}{{/cafe}}
                            </div>
                            <div class="small">{{#cafe}}{{addressRoad}}{{/cafe}}</div>
                        </div>
                        <div class="d-flex gap-2">
                            <a class="btn btn-sm btn-outline-secondary" href="/cafes/{{cafe.id}}/reviews/new">리뷰작성</a>

                            <button class="btn btn-sm btn-outline-secondary" data-bs-toggle="modal" data-bs-target="#favConfirmModal">
                                즐겨찾기
                            </button>

                            <a class="btn btn-sm btn-olive disabled">예약</a>
                        </div>

                        <div class="modal fade" id="favConfirmModal" tabindex="-1" aria-hidden="true">
                            <div class="modal-dialog modal-dialog-centered"><div class="modal-content">
                                <div class="modal-header">
                                    <h6 class="modal-title">즐겨찾기</h6>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="닫기"></button>
                                </div>
                                <div class="modal-body">
                                    {{#isFav}}즐겨찾기를 해제하시겠습니까?{{/isFav}}
                                    {{^isFav}}즐겨찾기에 추가하시겠습니까?{{/isFav}}
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">아니요</button>
                                    <form method="post" action="/cafes/{{cafe.id}}/favorite" class="m-0">
                                        {{#_csrf}}<input type="hidden" name="{{parameterName}}" value="{{token}}">{{/_csrf}}
                                        <button type="submit" class="btn btn-olive">네</button>
                                    </form>
                                </div>
                            </div></div>
                        </div>
                    <!-- 썸네일 레일 -->
                    <div class="row g-2 mt-3 thumb-rail">
                        {{#images}}
                            <div class="col-4"><img src="{{.}}" alt=""></div>
                        {{/images}}
                    </div>
                </div>
            </div>
        </div>

        <!-- 우측 지도 -->
        <div class="col-12 col-lg-4">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <h6 class="mb-2">위치</h6>
                    {{#hasGeo}}
                        <div class="map-box d-flex align-items-center justify-content-center">
                            <div class="text-muted small">
                                지도 자리(연동 예정)<br>
                                lat: {{#cafe}}{{lat}}{{/cafe}}, lng: {{#cafe}}{{lng}}{{/cafe}}
                            </div>
                        </div>
                    {{/hasGeo}}
                    {{^hasGeo}}
                        <div class="alert alert-light border small mb-0">
                            아직 좌표 정보가 없습니다.
                        </div>
                    {{/hasGeo}}
                </div>
            </div>
        </div>
    </div>

    <!-- 중단: 매장정보 / 메뉴 -->
    <div class="row g-3">
        <div class="col-12 col-lg-8">
            <div class="row g-3">
                <div class="col-md-6">
                    <div class="card shadow-sm h-100">
                        <div class="card-body">
                            <h6 class="mb-3">매장 정보</h6>
                            <ul class="list-unstyled small mb-0">
                                <li>📍 {{#cafe}}{{addressRoad}}{{/cafe}}</li>
                                <li>📞 {{#cafe}}{{#phone}}{{.}}{{/phone}}{{^phone}}-{{/phone}}{{/cafe}}</li>
                                <li>🕒 영업시간: (추가 예정)</li>
                                <li>기타: {{#cafe}}{{#description}}{{.}}{{/description}}{{^description}}-{{/description}}{{/cafe}}</li>
                            </ul>
                        </div>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="card shadow-sm h-100">
                        <div class="card-body">
                            <h6 class="mb-3">메뉴</h6>
                            {{#menus}}
                                <div class="d-flex justify-content-between small">
                                    <span>{{name}}</span><span>{{price}}</span>
                                </div>
                            {{/menus}}
                            {{^menus}}
                                <div class="small text-muted">등록된 메뉴가 없습니다.</div>
                            {{/menus}}
                        </div>
                    </div>
                </div>
            </div>

            <!-- 리뷰 -->
            <div class="card shadow-sm mt-3">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-2">
                        <h6 class="m-0">리뷰</h6>
                        <div class="btn-group btn-group-sm">
                            <a class="btn btn-outline-secondary disabled">최신순</a>
                            <a class="btn btn-outline-secondary disabled">별점순</a>
                        </div>
                    </div>
                    {{#reviews}}
                        <div class="border rounded p-3 mb-3">
                            <div class="d-flex justify-content-between">
                                <div class="fw-semibold">★ {{rating}}</div>
                                <div class="small text-muted">{{createdAt}}</div>
                            </div>
                            <div class="small text-muted">작성자: {{nickname}}</div>
                            <div class="mt-2">{{content}}</div>

                            <div class="mt-2 d-flex gap-2 flex-wrap">
                                {{#imageUrl1}}<img src="{{.}}" class="rounded" style="width:96px;height:96px;object-fit:cover">{{/imageUrl1}}
                                {{#imageUrl2}}<img src="{{.}}" class="rounded" style="width:96px;height:96px;object-fit:cover">{{/imageUrl2}}
                                {{#imageUrl3}}<img src="{{.}}" class="rounded" style="width:96px;height:96px;object-fit:cover">{{/imageUrl3}}
                                {{#imageUrl4}}<img src="{{.}}" class="rounded" style="width:96px;height:96px;object-fit:cover">{{/imageUrl4}}
                                {{#imageUrl5}}<img src="{{.}}" class="rounded" style="width:96px;height:96px;object-fit:cover">{{/imageUrl5}}
                            </div>
                        </div>
                    {{/reviews}}
                    {{^reviews}}<div class="text-muted">아직 리뷰가 없어요.</div>{{/reviews}}
                </div>
            </div>
        </div>

        <!-- 우측: 베스트 -->
        <div class="col-12 col-lg-4">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h6 class="mb-3">베스트 (카페 핫한 곳)</h6>
                    {{#bestCafes}}
                        <a class="d-flex gap-3 text-decoration-none text-reset border rounded p-2 mb-2"
                           href="/cafes/{{id}}">
                            <div class="ratio ratio-16x9 bg-light" style="width:120px;background-image:url('{{#heroImageUrl}}{{.}}{{/heroImageUrl}}{{^heroImageUrl}}/images/placeholder_shop.jpg{{/heroImageUrl}}');background-size:cover;background-position:center;border-radius:.5rem"></div>
                            <div class="flex-grow-1">
                                <div class="fw-semibold text-truncate">{{name}}</div>
                                <div class="small text-muted text-truncate">{{addressRoad}}</div>
                                <div class="small mt-1">⭐ {{#averageRating}}{{.}}{{/averageRating}}{{^averageRating}}0.0{{/averageRating}} · 리뷰 {{reviewCount}}</div>
                            </div>
                        </a>
                    {{/bestCafes}}
                    {{^bestCafes}}
                        <div class="text-muted small">표시할 추천 매장이 없습니다.</div>
                    {{/bestCafes}}
                </div>
            </div>
        </div>
    </div>

</main>


{{> layout/footer}}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
