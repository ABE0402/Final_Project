<section class="mb-4">
    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 g-3">
        {{#cards}}
            <div class="col">
                <a class="card h-100 text-decoration-none text-reset" href="/{{pathSegment}}/{{id}}">
                    <div class="ratio ratio-16x9 card-img-top"
                         style="background-image:url('{{#heroImageUrl}}{{.}}{{/heroImageUrl}}{{^heroImageUrl}}/images/placeholder.jpg{{/heroImageUrl}}')"></div>
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-start">
                            <h6 class="card-title text-truncate mb-0">{{name}}</h6>
                            <span class="badge text-bg-success">{{type}}</span>
                        </div>
                        <div class="small text-muted text-truncate">{{address}}</div>
                        {{#description}}
                            <div class="mt-1 small text-muted line-clamp-2">{{description}}</div>
                        {{/description}}
                        <div class="mt-2 small">⭐ {{averageRating}} · 리뷰 {{reviewCount}}</div>
                        <div class="small text-muted mt-1">{{hashtags}}</div>
                    </div>
                </a>
            </div>
        {{/cards}}
        {{^cards}}
            <div class="col">
                <div class="alert alert-light border empty-message">아직 데이터가 없어요. 가게를 등록해 보세요!</div>
            </div>
        {{/cards}}
    </div>
</section>
