<!doctype html>
<html lang="ko">
<head>
    <meta charset="utf-8"><title>마이페이지 - 즐겨찾기</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/app.css" rel="stylesheet">
    <style>
        .thumb { width: 120px; height: 80px; border-radius: .5rem; object-fit: cover; background:#f3f3f3; }
    </style>
</head>
<body class="bg-body-tertiary">
{{> layout/header}}

<main class="container py-5">
    <h3 class="fw-bold mb-4">마이 페이지</h3>

    {{#msg}}<div class="alert alert-success">{{.}}</div>{{/msg}}

    <div class="row g-4">
        <aside class="col-12 col-md-3">{{> mypage/sidebar}}</aside>

        <section class="col-12 col-md-9">
            <div class="card shadow-sm">
                <div class="card-body p-4">
                    <h5 class="mb-3">즐겨찾기</h5>

                    {{#favorites}}
                        <div class="d-flex align-items-center gap-3 border rounded p-3 mb-2">
                            <img class="thumb"
                                 src="{{#heroImageUrl}}{{.}}{{/heroImageUrl}}{{^heroImageUrl}}/images/placeholder_shop.jpg{{/heroImageUrl}}"
                                 alt="">
                            <div class="flex-grow-1">
                                <div class="d-flex justify-content-between align-items-start">
                                    <div>
                                        <div class="fw-semibold">{{name}}</div>
                                        <div class="small text-muted text-truncate" style="max-width:520px;">{{#address}}{{.}}{{/address}}{{^address}}-{{/address}}</div>
                                        <div class="small mt-1">⭐ {{averageRating}} · 리뷰 {{reviewCount}}</div>
                                    </div>
                                    <div class="d-flex gap-2">
                                        <a class="btn btn-sm btn-outline-secondary" href="/cafes/{{cafeId}}">보기</a>
                                        <form method="post" action="/mypage/favorites/{{cafeId}}/remove" onsubmit="return confirm('즐겨찾기에서 제거하시겠습니까?');">
                                            {{#_csrf}}<input type="hidden" name="{{parameterName}}" value="{{token}}">{{/_csrf}}
                                            <button class="btn btn-sm btn-outline-danger" type="submit">삭제</button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {{/favorites}}

                    {{^favorites}}
                        <div class="alert alert-light border">즐겨찾기한 가게가 없습니다.</div>
                    {{/favorites}}
                </div>
            </div>
        </section>
    </div>
</main>

{{> layout/footer}}
</body>
</html>
