<!doctype html>
<html lang="ko"><head>
    <meta charset="utf-8"><title>점주 신청 상세</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head><body class="bg-body-tertiary">
{{> layout/header}}

<main class="container py-5" style="max-width:800px;">
    <h4 class="mb-3">점주 신청 상세</h4>

    <div class="card mb-3">
        <div class="card-body">
            <div><strong>ID:</strong> {{app.id}}</div>
            <div><strong>사용자 ID:</strong> {{app.userId}}</div>
            <div><strong>유형:</strong> {{app.storeType}}</div>
            <div><strong>가게명:</strong> {{app.storeName}}</div>
            <div><strong>사업자번호:</strong> {{app.businessNumber}}</div>
            <div><strong>대표자 본명:</strong> {{app.ownerRealName}}</div>
            <div><strong>연락처:</strong> {{app.contactPhone}}</div>
            <div><strong>주소:</strong> {{app.storeAddress}}</div>
            <div><strong>상태:</strong> {{app.status}}</div>
            {{#app.rejectionReason}}<div><strong>반려사유:</strong> {{.}}</div>{{/app.rejectionReason}}
            <div class="text-muted small mt-2">신청일: {{app.createdAt}}</div>
        </div>
    </div>

    <div class="d-flex gap-2">
        <form method="post" action="/admin/owner-applications/{{app.id}}/approve">
            {{#_csrf}}<input type="hidden" name="{{parameterName}}" value="{{token}}">{{/_csrf}}
            <input type="hidden" name="note" value="">
            <button class="btn btn-success" type="submit">승인</button>
        </form>

        <form method="post" action="/admin/owner-applications/{{app.id}}/reject" class="d-flex gap-2">
            {{#_csrf}}<input type="hidden" name="{{parameterName}}" value="{{token}}">{{/_csrf}}
            <input type="text" name="reason" class="form-control" placeholder="반려 사유" required>
            <button class="btn btn-danger" type="submit">반려</button>
        </form>
    </div>
</main>

{{> layout/footer}}
</body></html>
