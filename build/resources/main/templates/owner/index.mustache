<!doctype html>
<html lang="ko">
<head>
    <meta charset="utf-8"><title>점주 페이지</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/app.css" rel="stylesheet">
</head>
<body class="bg-body-tertiary">
{{> layout/header}}

<main class="container py-5">
    <h3 class="fw-bold mb-4">점주 페이지</h3>
    <div class="row g-4">
        <aside class="col-12 col-md-3">
            <div class="card shadow-sm"><div class="card-body p-0">
                {{> owner/sidebar}}
            </div></div>
        </aside>

        <section class="col-12 col-md-9">
            <!-- 리뷰 관리 -->
            {{#tabOwnerReviews}}
                <div class="card shadow-sm"><div class="card-body p-4">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <h5 class="m-0">리뷰 관리</h5>
                        <div class="btn-group btn-group-sm">
                            <a class="btn btn-outline-secondary disabled">전체</a>
                            <a class="btn btn-outline-secondary disabled">신고됨</a>
                        </div>
                    </div>
                    {{#reviews}}
                        <div class="border rounded p-3 mb-2">
                            <div class="fw-semibold">{{title}}</div>
                            <div class="small text-muted">{{createdAt}}</div>
                            <div class="mt-2">{{content}}</div>
                            <div class="mt-2 d-flex gap-2">
                                <a class="btn btn-sm btn-outline-secondary disabled">답글달기</a>
                                <a class="btn btn-sm btn-outline-danger disabled">신고</a>
                            </div>
                        </div>
                    {{/reviews}}
                    {{^reviews}}<div class="text-muted">표시할 리뷰가 없습니다.</div>{{/reviews}}
                </div></div>
            {{/tabOwnerReviews}}

            <!-- 매장 관리 (내 매장 목록) -->
            {{#tabOwnerShops}}
                <div class="card shadow-sm"><div class="card-body p-4">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <h5 class="m-0">매장 관리</h5>
                        <a class="btn btn-olive" href="/owner/shops/new">가게 등록</a>
                    </div>
                    {{#shops}}
                        <div class="border rounded p-3 mb-2">
                            <div class="fw-semibold">{{name}} <span class="badge text-bg-light">{{type}}</span></div>
                            <div class="small text-muted">{{address}} · {{phone}}</div>
                            <div class="mt-2 d-flex gap-2">
                                <a class="btn btn-sm btn-outline-secondary disabled">정보 수정</a>
                                <a class="btn btn-sm btn-outline-secondary disabled">리뷰 보기</a>
                            </div>
                        </div>
                    {{/shops}}
                    {{^shops}}<div class="text-muted">등록된 매장이 없습니다. 우측 상단 ‘가게 등록’을 눌러 등록해 주세요.</div>{{/shops}}
                </div></div>
            {{/tabOwnerShops}}

            <!-- 예약 관리 -->
            {{#tabOwnerReservations}}
                <div class="card shadow-sm"><div class="card-body p-4">
                    <h5 class="mb-3">예약 관리</h5>
                    {{#reservations}}
                        <!-- TODO: 예약 카드 -->
                    {{/reservations}}
                    {{^reservations}}<div class="text-muted">예약 내역이 없습니다.</div>{{/reservations}}
                </div></div>
            {{/tabOwnerReservations}}
        </section>
    </div>
</main>

{{> layout/footer}}
</body>
</html>