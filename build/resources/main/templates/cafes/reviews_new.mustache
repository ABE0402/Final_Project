<!doctype html>
<html lang="ko">
<head>
    <meta charset="utf-8"><title>리뷰 작성</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-body-tertiary">
{{> layout/header}}

<main class="container py-5" style="max-width:720px;">
    <h4 class="fw-bold mb-3">리뷰 작성 · {{cafe.name}}</h4>

    <form method="post" action="/cafes/{{cafe.id}}/reviews" enctype="multipart/form-data" class="card shadow-sm">
        <div class="card-body p-4">
            {{#_csrf}}<input type="hidden" name="{{parameterName}}" value="{{token}}">{{/_csrf}}

            <div class="mb-3">
                <label class="form-label">평점</label>
                <select name="rating" class="form-select" required>
                    <option value="5">5 - 최고</option>
                    <option value="4">4</option>
                    <option value="3" selected>3</option>
                    <option value="2">2</option>
                    <option value="1">1 - 별로</option>
                </select>
            </div>

            <div class="mb-3">
                <label class="form-label">내용</label>
                <textarea name="content" rows="5" class="form-control" placeholder="방문 경험을 적어주세요." required></textarea>
            </div>

            <div class="mb-3">
                <label class="form-label">사진 첨부 (최대 5장)</label>
                <input class="form-control" type="file" name="images" accept="image/*" multiple>
                <div class="form-text">여러 장 선택 가능</div>
            </div>

            <div class="d-flex">
                <a class="btn btn-outline-secondary" href="/cafes/{{cafe.id}}">돌아가기</a>
                <button class="btn btn-olive ms-auto" type="submit">등록</button>
            </div>
        </div>
    </form>
</main>

{{> layout/footer}}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
