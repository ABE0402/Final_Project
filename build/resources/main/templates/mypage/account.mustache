<!doctype html>
<html lang="ko">
<head>
    <meta charset="utf-8"><title>마이페이지 - 계정관리</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/app.css" rel="stylesheet">
</head>
<body class="bg-body-tertiary">
{{> layout/header}}

<main class="container py-5">
    <h3 class="fw-bold mb-4">계정 관리</h3>
    <div class="row g-4">
        <aside class="col-12 col-md-3">{{> mypage/sidebar}}</aside>

        <section class="col-12 col-md-9">
            <div class="row g-3">
                <!-- 내 계정 정보 -->
                <div class="col-12">
                    <div class="card shadow-sm">
                        <div class="card-body">
                            <h5 class="mb-3">내 계정</h5>
                            <div class="row">
                                <div class="col-md-6">
                                    <div class="mb-2"><span class="text-muted">이메일</span><div>{{me.email}}</div></div>
                                    <div class="mb-2"><span class="text-muted">닉네임</span><div>{{me.nickname}}</div></div>
                                </div>
                                <div class="col-md-6">
                                    <div class="mb-2"><span class="text-muted">역할</span>
                                        <div>
                                            {{#auth.isAdmin}}<span class="badge text-bg-warning">ADMIN</span>{{/auth.isAdmin}}
                                            {{#auth.isOwner}}<span class="badge text-bg-success">OWNER</span>{{/auth.isOwner}}
                                            {{^auth.isOwner}}{{^auth.isAdmin}}<span class="badge text-bg-secondary">USER</span>{{/auth.isAdmin}}{{/auth.isOwner}}
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- 비밀번호 변경 -->
                <div class="col-12">
                    <div class="card shadow-sm">
                        <div class="card-body">
                            <h5 class="mb-3">비밀번호 변경</h5>
                            {{#pwChanged}}<div class="alert alert-success">비밀번호가 변경되었습니다.</div>{{/pwChanged}}
                            {{#pwError}}<div class="alert alert-danger">{{.}}</div>{{/pwError}}
                            <form method="post" action="/mypage/account/password">
                                {{#_csrf}}<input type="hidden" name="{{parameterName}}" value="{{token}}">{{/_csrf}}
                                <div class="row g-2">
                                    <div class="col-md-4"><input type="password" name="currentPassword" class="form-control" placeholder="현재 비밀번호" required></div>
                                    <div class="col-md-4"><input type="password" name="newPassword" class="form-control" placeholder="새 비밀번호" required></div>
                                    <div class="col-md-4"><input type="password" name="confirmPassword" class="form-control" placeholder="새 비밀번호 확인" required></div>
                                </div>
                                <div class="mt-3 text-end">
                                    <button class="btn btn-olive" type="submit">변경</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>

                <!-- 점주 신청 영역 -->
                <div class="col-12">
                    <div class="card shadow-sm">
                        <div class="card-body">
                            <div class="d-flex justify-content-between align-items-center mb-2">
                                <h5 class="m-0">점주 신청</h5>
                                <a class="btn btn-outline-olive" href="/owner/apply">점주 신청하기</a>
                            </div>
                            <div class="table-responsive">
                                <table class="table table-sm align-middle">
                                    <thead><tr><th>ID</th><th>유형</th><th>가게명</th><th>사업자번호</th><th>상태</th><th>신청일</th></tr></thead>
                                    <tbody>
                                    {{#apps}}
                                        <tr>
                                            <td>{{id}}</td>
                                            <td>{{storeType}}</td>
                                            <td>{{storeName}}</td>
                                            <td>{{businessNumber}}</td>
                                            <td>
                                                {{status}}
                                                {{#rejectionReason}}<span class="text-danger">(반려: {{.}})</span>{{/rejectionReason}}
                                            </td>
                                            <td>{{createdAt}}</td>
                                        </tr>
                                    {{/apps}}
                                    {{^apps}}
                                        <tr><td colspan="6" class="text-center text-muted">신청 이력이 없습니다.</td></tr>
                                    {{/apps}}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>

            </div>
        </section>
    </div>
</main>

{{> layout/footer}}
</body>
</html>
