<!doctype html>
<html lang="ko">
<head>
    <meta charset="utf-8"><title>마이페이지 - 프로필관리</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/app.css" rel="stylesheet">
</head>
<body class="bg-body-tertiary">
{{> layout/header}}

<main class="container py-5">
    <h3 class="fw-bold mb-4">마이 페이지</h3>
    {{#saved}}<div class="alert alert-success">프로필이 저장되었습니다.</div>{{/saved}}
    {{#errorMessage}}<div class="alert alert-danger">{{.}}</div>{{/errorMessage}}

    <div class="row g-4">
        <aside class="col-12 col-md-3">{{> mypage/sidebar}}</aside>

        <!-- 우측 콘텐츠 -->
        <section class="col-12 col-md-9">
            <div class="card shadow-sm">
                <div class="card-body p-4">
                    <h5 class="mb-3">프로필 수정</h5>

                    <form method="post" action="/mypage/profile" enctype="multipart/form-data" class="needs-validation" novalidate>
                        {{#_csrf}}<input type="hidden" name="{{parameterName}}" value="{{token}}">{{/_csrf}}

                        <div class="row g-3 align-items-center mb-2">
                            <div class="col-auto">
                                <div class="photo-box">
                                    <img id="photoPreview"
                                         src="{{#me}}{{#profileImageUrl}}{{.}}{{/profileImageUrl}}{{^profileImageUrl}}/images/profile-placeholder.png{{/profileImageUrl}}{{/me}}{{^me}}/images/profile-placeholder.png{{/me}}"
                                         onerror="this.onerror=null;this.src='/images/profile-placeholder.png'"
                                         alt="미리보기" class="w-100 h-100 object-fit-cover rounded">
                                </div>
                            </div>
                            <div class="col">
                                <label class="form-label">프로필 사진</label>
                                <input class="form-control" type="file" name="photo" accept="image/*" onchange="previewPhoto(event)">
                                <div class="form-text">정사각형 이미지를 권장합니다.</div>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">닉네임</label>
                            <input type="text" name="nickname" class="form-control" value="{{#me}}{{nickname}}{{/me}}" placeholder="별명">
                        </div>

                        <div class="row g-3">
                            <div class="col-md-6">
                                <label class="form-label">생년월일</label>
                                <input type="date" name="birthDate" class="form-control" value="{{#me}}{{birthDate}}{{/me}}">
                            </div>
                            <div class="col-md-6">
                                <label class="form-label d-block">성별</label>
                                <div class="btn-group" role="group" aria-label="gender">
                                    <input type="radio" class="btn-check" name="gender" id="g-male" value="MALE" {{#genderMale}}checked{{/genderMale}}>
                                    <label class="btn btn-outline-olive" for="g-male">남성</label>

                                    <input type="radio" class="btn-check" name="gender" id="g-female" value="FEMALE" {{#genderFemale}}checked{{/genderFemale}}>
                                    <label class="btn btn-outline-olive" for="g-female">여성</label>
                                </div>
                            </div>
                        </div>

                        <div class="mt-3">
                            <label class="form-label">소개</label>
                            <textarea class="form-control" name="bio" rows="3" placeholder="간단한 소개를 적어보세요.">{{#me}}{{#bio}}{{.}}{{/bio}}
                                {{^bio}}<!-- 비어있음 -->{{/bio}}{{/me}}</textarea>
                        </div>

                        <div class="mt-4 d-flex">
                            <button class="btn btn-olive ms-auto" type="submit">저장</button>
                        </div>
                    </form>
                </div>
            </div>
        </section>
    </div>
</main>

{{> layout/footer}}

<script>
    function previewPhoto(e){
      const [file] = e.target.files || [];
      if (!file) return;
      const img = document.getElementById('photoPreview');
      img.src = URL.createObjectURL(file);
    }
</script>
</body>
</html>
